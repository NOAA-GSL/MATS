apiVersion: v1
kind: Service
metadata:
  name: cb-raob
  labels:
    app: cb-raob
spec:
  selector:
    app: cb-raob
  ports:
  - port: 80
    targetPort: 9000
    protocol: TCP
    name: http
  type: ClusterIP
